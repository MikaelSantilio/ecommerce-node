# API Gateway Configuration
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Request Limits
JSON_LIMIT=10mb
URL_ENCODED_LIMIT=10mb

# Service URLs
AUTH_SERVICE_URL=http://localhost:3001
CATALOG_SERVICE_URL=http://localhost:3002
CART_SERVICE_URL=http://localhost:3003
ORDERS_SERVICE_URL=http://localhost:3004
PAYMENTS_SERVICE_URL=http://localhost:3005
NOTIFICATIONS_SERVICE_URL=http://localhost:3006

# Redis Configuration (opcional para rate limiting avançado)
REDIS_URL=redis://localhost:6379

# 🔒 SECURITY - JWT interno para comunicação entre serviços
# CRÍTICO: Use chaves seguras em produção! 
# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
INTERNAL_JWT_SECRET=your-super-secret-internal-jwt-key-change-in-production-64-chars-minimum
GATEWAY_SECRET=your-super-secret-gateway-signature-key-change-in-production-64-chars

# Network Security - IPs permitidos para acessar microserviços
# Em produção, configure apenas o IP do Gateway
ALLOWED_GATEWAY_IPS=127.0.0.1,::1,localhost,172.18.0.0/16,10.0.0.0/8

# JWT Configuration (se usar validação local no futuro)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
