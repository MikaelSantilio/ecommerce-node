<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Pedido Enviado - E-commerce</title>
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: #2196F3; color: white; padding: 20px; text-align: center; }
    .content { padding: 20px; background: #f9f9f9; }
    .tracking-info { background: white; padding: 15px; margin: 15px 0; border-radius: 5px; border-left: 4px solid #2196F3; }
    .footer { text-align: center; padding: 20px; color: #666; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>🚚 Pedido Enviado!</h1>
    </div>

    <div class="content">
      <p>Olá <strong>{{user.name}}</strong>,</p>
      <p>Seu pedido <strong>#{{order.id}}</strong> foi enviado e está a caminho!</p>

      <div class="tracking-info">
        <h3>📦 Informações de Rastreamento</h3>
        {{#if order.shipping.trackingCode}}
        <p><strong>Código de Rastreamento:</strong> {{order.shipping.trackingCode}}</p>
        <p><strong>Transportadora:</strong> {{order.shipping.carrier}}</p>
        {{/if}}
        {{#if order.shipping.estimatedDate}}
        <p><strong>Previsão de Entrega:</strong> {{formatDate order.shipping.estimatedDate}}</p>
        {{/if}}
      </div>

      <p>Para acompanhar a entrega em tempo real, acesse: <a href="{{trackingUrl}}">{{trackingUrl}}</a></p>

      <p>Atenciosamente,<br>
      <strong>Equipe E-commerce</strong></p>
    </div>

    <div class="footer">
      <p>Este é um email automático, por favor não responda.</p>
      <p>© 2025 E-commerce. Todos os direitos reservados.</p>
    </div>
  </div>
</body>
</html>
